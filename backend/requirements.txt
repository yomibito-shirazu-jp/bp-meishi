fastapi==0.115.0
uvicorn[standard]==0.30.0
pydantic==2.7.0
python-multipart==0.0.9
httpx==0.27.0
pymupdf==1.24.2
google-generativeai>=0.8.3
google-cloud-documentai
google-cloud-vision
Pillow
markitdown[pdf]
markdown

# Optional: YomiToku (高精度日本語OCR・レイアウト/縦書き/手書き対応)
# DN_SuperBook_PDF_Converter と同じエンジン。GPU があれば高速。
# 重いので導入は任意:  pip install yomitoku
# yomitoku>=0.10.1

# Optional: docling (https://github.com/docling-project/docling)
# IBM の ML ベース文書構造解析エンジン。PDF/DOCX/PPTX 等に対応。
# ML モデルが大きいので導入は任意:  pip install docling
# docling>=2.0.0
