fastapi==0.115.0
uvicorn[standard]==0.30.0
pydantic>=2.9.2
python-multipart==0.0.9
httpx==0.27.0
pymupdf==1.24.2
google-generativeai>=0.8.3
google-cloud-documentai
google-cloud-vision
Pillow
markitdown[pdf]
markdown

# YomiToku (高精度日本語OCR・レイアウト/縦書き/手書き対応)
# DN_SuperBook_PDF_Converter と同じエンジン。
yomitoku>=0.10.1

# docling (https://github.com/docling-project/docling)
# IBM の ML ベース文書構造解析エンジン。PDF/DOCX/PPTX 等に対応。
# 雑誌プロファイルで yomitoku が未インストール時のフォールバックとして使用。
docling>=2.0.0

# Optional: huridocs/pdf-document-layout-analysis (https://github.com/huridocs/pdf-document-layout-analysis)
# VGT / LayoutLMv3 ベースの DL レイアウト解析。別コンテナで REST サービスとして起動し URL 指定:
#   docker run -p 5060:5060 huridocs/pdf-document-layout-analysis:latest
#   export HURIDOCS_URL=http://localhost:5060
# (httpx は既に上で fastapi と一緒に含まれている)
